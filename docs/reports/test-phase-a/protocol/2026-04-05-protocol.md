# Protocol パッケージ テストギャップ分析

**日付**: 2026-04-05
**パッケージ**: `@detonator/protocol`
**フェーズ**: A（既存テスト検証・補完）
**分析エージェント**: explore agent (ses_2a3d2c970ffeE8xqwBrIDHLGsu)

---

## 1. スコープ

対象:
- ソース: `types.ts`（15列挙, 9インターフェース）, `commands.ts`（7ペイロード）, `events.ts`（38ペイロード）, `timers.ts`（7タイプ + QueueEntry共用体）, `constants.ts`, `index.ts`
- テスト: `protocol.constants.test.ts`, `protocol.type-exports.test.ts`
- 権威: `api.md`（最終）, `shared-dev-plan.md` §4（二次）

> **注**: 要望では16列挙だったが、実際は **15列挙**。コードは api.md と一致。

---

## 2. 権威チェーン

| 優先 | ドキュメント | 使用セクション |
|---|---|---|
| 1（最終） | `api.md` | 列挙定義、コマンド/イベント/タイマー契約、共用インターフェース |
| 2 | `shared-dev-plan.md` | §4.3–§4.10（ファイル責任、シグネチャ） |

---

## 3. 既存カバレッジ目録

### `protocol.constants.test.ts`（4テスト）

| # | テスト名 | 実際の検証内容 | 品質 |
|---|---|---|---|
| 1 | has expected counts | `ALL_COMMAND_NAMES.length === 7`, events=38, rooms=2 | 🟡 数値のみ、メンバーシップ確認なし |
| 2 | no duplicate public names | コマンド/イベント/ルーム内および跨りの一意性 | ✅ 最強のテスト |
| 3 | freezes name maps | ALL_* を同一オブジェクトから導出 — **同義反復的**、freezeを検証していない | 🔴 名前が誤解を招く |
| 4 | stable enum cardinalities | ItemType=14, SkillType=14, ErrorCode=18 | 🟡 15列挙中わず3つのみ |

### `protocol.type-exports.test.ts`（2テスト）

| # | テスト名 | 実際の検証内容 | 品質 |
|---|---|---|---|
| 5 | exports runtime symbols | バレルエクスポートキーの存在（20シンボル） | ✅ ドリフト検出に有意義 |
| 6 | exports compile-time contracts | `toMatchTypeOf`（弱）、単一リテラルスポットチェック、`not.toBeAny` | 🟡 意図は良いがアサーションが弱い |

### サマリー
- **合計6テスト**、全て正常系
- **0件のネガティブコンパイル時テスト**
- **0件の正確なフィールド形状整合性テスト**
- **1件の同義反復的テスト**

---

## 4. ギャップ分析マトリックス

| カテゴリ | 状態 | 現在の担保 | 重要なギャップ |
|---|---|---|---|
| **列挙（15）** | 🟡 一部カバー | 3/15のみ数値チェック | どの列挙メンバーも正確値テストなし |
| **コマンド（7）** | 🟡 一部カバー | 全7つがエクスポート | 形状/オプショナル/フィールド名テストなし |
| **イベント（38）** | 🟡 一部カバー | 数=38、全エクスポート | どのイベントもフィールド形状テストなし |
| **タイマー（7+1）** | 🟡 一部カバー | 共用体が概ね一致 | 弱い`toMatchTypeOf`；判別子テストなし |
| **定数** | 🟡 一部カバー | 数値 + 一意性 | 同義反復的freezeテスト；リテラルリスト整合性なし |

---

## 5. 矛盾レポート ⚠️

| # | 問題 | 仕様書 | コード/ドキュメント | 重大度 |
|---|---|---|---|---|
| C-1 | 古い二次ドキュメント | — | `shared-dev-plan.md` は `ItemDestroyReason.FloorCleared` と `ErrorCode.DetonateOutOfRange` を欠落 | 低（コード自体は api.md と一致） |
| C-2 | `ItemDestroyReason.FloorCleared` の使用 | 列挙に含まれる | イベント記述では unmanaged-explosion / erosion のみがトリガーとされる | 中 — 列挙では許容される値だが発火される可能性が低い |
| C-3 | `evacuation` の挙動 | ランダムな生存プレイヤー近くへテレポート（リスポーン配置ロジック） | items.json の説明は「現在のリスポーン地点へ瞬間移動」 | 高 — ただしプロトコルペイロード形状にはエンコードされていない |

---

## 6. 曖昧点レポート ❓

| # | 質問 | 影響 |
|---|---|---|
| A-1 | `InventorySlot`: 型は `itemType: null, stackCount: 3` を許可するが仕様的に無効 | 型モデルの制限 |
| A-2 | `EffectExpiryEntry.effectType: string` はオープンエンド — テストで制約すべきか？ | 過剛制リスク |
| A-3 | `discard_item` の無効フェーズ拒否: 無視かエラーコードか？ | 専用 ErrorCode が未定義 |

---

## 7. 補完計画

### 新規ファイル: `protocol.enums.spec-d.test.ts` — 正確な列挙値整合性テスト（15テスト）

| rule_id | 列挙 | アサート内容 |
|---|---|---|
| P-ENUM-01 | CellType | Safe=0, SafeMine=1, DangerousMine=2, Wasteland=3, Hole=4 |
| P-ENUM-02 | GamePhase | Playing=0, FloorClearTransition=1, Rest=2, GameOver=3 |
| P-ENUM-03 | PlayerLifeState | Alive=0, Ghost=1, Disconnected=2 |
| P-ENUM-04 | Facing8 | N=0..NW=7（全8方向） |
| P-ENUM-05 | Facing4 | N=0..W=3（全4方向） |
| P-ENUM-06 | DeathCause | UnmanagedExplosion=0, Erosion=1, Event=2 |
| P-ENUM-07 | ItemType | 全14文字列値の完全一致 |
| P-ENUM-08 | SkillType | 全14文字列値の完全一致 |
| P-ENUM-09 | LeaveReason | voluntary, timeout |
| P-ENUM-10 | FuseCancelReason | source_removed, mine_removed, flag_removed, floor_cleared |
| P-ENUM-11 | ErosionWarningCancelReason | take_a_breath, short_break, floor_cleared |
| P-ENUM-12 | ExpSource | dig, detonate_combo |
| P-ENUM-13 | ItemDestroyReason | unmanaged_explosion, erosion, floor_cleared |
| P-ENUM-14 | GameOverReason | all_dead, floor_10_cleared |
| P-ENUM-15 | ErrorCode | 全18文字列の完全一致、特に DETONATE_OUT_OF_RANGE |

### 新規ファイル: `protocol.commands.spec-d.test.ts` — コマンド形状テスト（7+テスト）

| rule_id | コマンド | 主要アサーション |
|---|---|---|
| P-CMD-01 | MovePayload | `{ vx: number; vy: number }` の厳密一致; 否定: `{ x, y }` は拒否 |
| P-CMD-02 | DigPayload | `{ x: number; y: number }` の厳密一致 |
| P-CMD-03 | FlagPayload | `{ x: number; y: number }` の厳密一致 |
| P-CMD-04 | DetonatePayload | `{ x: number; y: number }` の厳密一致 |
| P-CMD-05 | UseItemPayload | `{ slotIndex: number; targetCoord?: GridCoord }` — オプショナルフィールドが重要 |
| P-CMD-06 | DiscardItemPayload | `{ slotIndex: number }` の厳密一致 |
| P-CMD-07 | ClaimRewardPayload | `{ offerId: string; optionIndex: number }` の厳密一致 |

### 新規ファイル: `protocol.events.spec-d.test.ts` — イベント形状テスト（カテゴリ別）

| rule_id | カテゴリ | 対象イベント（38総数） |
|---|---|---|
| P-EVT-01 | 汎用/エラー | ErrorEvent, PlayerJoinedEvent, PlayerLeftEvent, PlayerDisconnectedEvent, PlayerReconnectedEvent |
| P-EVT-02 | 起爆（5） | DetonatePreviewEvent～DetonateResolvedEvent |
| P-EVT-03 | 管理外爆発（3） | UnmanagedExplosionTriggeredEvent～UnmanagedExplosionResolvedEvent |
| P-EVT-04 | 侵食（3） | ErosionWarningEvent～ErosionAppliedEvent |
| P-EVT-05 | CP（3） | CatsEyeActivatedEvent～CpCollectedEvent |
| P-EVT-06 | 進行（3） | ExpGainedEvent, LevelUpEvent, RewardOfferEvent |
| P-EVT-07 | アイテム（7） | ItemDroppedEvent～InventoryUpdatedEvent |
| P-EVT-08 | 死亡/リスポーン（5） | PlayerDeathEvent～GameOverEvent |
| P-EVT-09 | フロア/スコア（4） | FloorClearedEvent～ScoreUpdatedEvent |

### 新規ファイル: `protocol.timers.spec-d.test.ts` — タイマー形状テスト（8テスト）

| rule_id | タイマー | 主要アサーション |
|---|---|---|
| P-TMR-01 | DetonateFuseEntry | 厳密形状 + 判別子 `"detonate_resolve"` |
| P-TMR-02 | UnmanagedChainEntry | 厳密形状 + 判別子 `"unmanaged_chain"` |
| P-TMR-03 | ErosionPhaseEntry | `"erosion_warn" \| "erosion_convert"` 共用体判別子 |
| P-TMR-04〜07 | ItemExpiry, Respawn, EffectExpiry, FutureEvent | 厳密形状 |
| P-TMR-08 | QueueEntry | `toMatchTypeOf` → `toEqualTypeOf` へ厳密な共用体置換 |

### 既存ファイルへの強化

| ファイル | 変更内容 | 理由 |
|---|---|---|
| constants.test.ts #3 | "freezes" テストを書き換え | 現在同義反復的；真の `Object.isFrozen` をテストするか名称変更 |
| constants.test.ts #4 | 全15列挙へ拡張 | 現在は 3/15 のみ |
| type-exports.test.ts #6 | `toMatchTypeOf` → `toEqualTypeOf` へアップグレード | 狭い共用体ドリフト防止 |

---

## 8. 既存テスト品質判定

| テスト | 判定 | 理由 |
|---|---|---|
| constants#1（数値） | **書き換え** | 正確なメンバーシップアサーション追加 |
| constants#2（重複なし） | **維持** | スイート内で最強力 |
| constants#3（freeze） | **書き換え** | 同義反復的 + 誤解を招く名称 |
| constants#4（列挙数値） | **書き換え** | 全15列挙を正確値でカバー |
| type-exports#5（ランタイム） | **維持** | 良好なバレル面回帰テスト |
| type-exports#6（コンパイル時） | **書き換え** | アサーション強度アップグレード |

**全体評価**: エクスポート表面のカバレッジは存在するが、権威ある契約カバレッジではない。最大の未防備リスク: 列挙リテラルドリフト、フィールド名ドリフト、オプショナル/必須ドリフト。

---

## 決定ログ

*（このパッケージでは現時点で人間の判断は不要）*
